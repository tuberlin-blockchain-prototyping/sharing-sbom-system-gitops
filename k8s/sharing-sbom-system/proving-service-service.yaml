apiVersion: v1
kind: Service
metadata:
  name: proving-service
  namespace: sharing-sbom-system
spec:
  selector:
    app: proving-service
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  type: ClusterIP

